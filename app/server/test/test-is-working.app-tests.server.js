import {assert} from 'meteor/practicalmeteor:chai';


describe('Test on server', () => {
  describe('is working', () => {
    it('test is working', () => {
      assert.equal(true, true);
    });
  });
});
