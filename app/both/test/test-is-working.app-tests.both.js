import {assert} from 'meteor/practicalmeteor:chai';


describe('B : Test on client and server', () => {
  describe('is working', () => {
    it('test is working', () => {
      assert.equal(true, true);
    });
  });
});

